vector<int> values;
for(int i = 0; i < 10; i++) {
    values.push_back(rand() % 200);
}
//The above code will create a vector with ten values where each value is a random number between zero and two hundred.

